# Copy to private/secrets.env (never commit). Reserved for future
# service tokens or dashboard credentials.
# DASHBOARD_USER=
# DASHBOARD_PASSWORD=
# NAS credentials stay in the existing PowerShell SAN config, not here.
# NAS_USER=
# NAS_PASSWORD=
